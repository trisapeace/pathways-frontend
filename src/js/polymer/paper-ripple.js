require('paper-ripple/paper-ripple.html');

import PolymerComponent from 'polymer/util/PolymerComponent';

export class PaperRipple extends PolymerComponent {
    static options = {
        element: "paper-ripple"
    };
}
