require('paper-badge/paper-badge.html');

import PolymerComponent from 'polymer/util/PolymerComponent';

export class PaperBadge extends PolymerComponent {
    static options = {
        element: "paper-badge"
    };
}
